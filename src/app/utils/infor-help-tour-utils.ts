export const templateHelp = (): any =>{
  return{
    "title": "Campo para subir un archivo HTML y CSS",
    "text": "Arrastre los archivos a ser cargados para ser empleados como un map visual, pulse el boton de cargar documento y espere que se genere la visual, guarde la plantilla estática para su posterior uso."
  }
}

export const informationDataGenerationHelp = (): any =>{
  return{
    "title": "Campo para interactuar con el multimodelo de generación de texto",
    "text": "Basicamente es un chat para gestionar la información escrita requerida para formar una imagen."
  }
}

export const informationImageGenerationHelp = (): any =>{
  return{
    "title": "Campo para interactuar con el modelo de generación de imagen",
    "text": "Es un campo para crear o insertar el prompt requerido por el modelo para crear una imagen."
  }
}